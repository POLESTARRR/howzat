# Copy to .streamlit/secrets.toml (gitignored) for local testing.
# On Streamlit Community Cloud, paste this into the app's Secrets settings
# instead -- same key, same name.
GOOGLE_API_KEY = "AQ...."
